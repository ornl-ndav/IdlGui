function getCombinedSpectrum, nexusfile, ALL=ALL

  ; Open the NeXus file
  
  
  ; Determine which detector banks for loop over 
  
  
  
  ; Loop over the detector banks
  
  ; Extract
  
  result = { filename: nexusfile }

  return, result
end
